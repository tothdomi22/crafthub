name: Build and push to GCP Artifact Registry
on:
  workflow_call:
    inputs:
      ENVIRONMENT:
        type: string
        required: true

env:
  REGION: europe-west6

jobs:
  build:
    environment: ${{ inputs.ENVIRONMENT }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        end: [ be ]

    steps:
      - uses: "actions/checkout@v4"

      - id: "auth"
        uses: "google-github-actions/auth@v2"
        with:
          credentials_json: "${{ secrets.GCP_KEY }}"
          export_environment_variables: true
          create_credentials_file: true

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v2"

      - name: Authenticate Docker
        run: |-
          gcloud --quiet auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          file: ${{ matrix.end }}.Dockerfile
          context: .
          push: true
          tags: |
            ${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/madebyme-artifacts/${{ matrix.end }}:${{ github.sha }}
            ${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/madebyme-artifacts/${{ matrix.end }}:latest
          cache-from: type=registry,ref=${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/madebyme-artifacts/${{ matrix.end }}:buildcache
          cache-to: type=registry,ref=${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/madebyme-artifacts/${{ matrix.end }}:buildcache,mode=max
          secrets: |
            google_credentials=${{ secrets.GCP_KEY }}
